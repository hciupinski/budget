import type { ReactNode } from "react";

export function AccountsGeneralPanel({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export function TransfersPanel({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export function SnapshotsPanel({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export function InvestmentsPanel({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

export function SavingsGoalsPanel({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
